# ============================================================
# Frontend .env  —  copy to Frontend/.env and fill in values
# VITE_ prefix required for Vite to expose to the browser.
# This file is baked into the build at compile time.
# ============================================================

# Backend API base URL — must be publicly reachable from user's browser
# On a VPS: use your public IP or domain (no trailing slash)
# Example: http://66.116.225.151:3001  OR  https://api.yourdomain.com
VITE_API_URL=http://YOUR_VPS_IP:3001

# Supabase (same project as backend)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
